# ---
# Category: Condition
# Since: 0.5.0
# Usage: l.is_gnu_sed
# Description: Return 0 (true) or 1 (false). This function should never throw exception error.
# ---

l.is_gnu_sed() {
  _lobash.is_gnu_sed
}
